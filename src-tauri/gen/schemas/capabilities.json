{"default":{"identifier":"default","description":"Core permissions for the main window: lets the frontend use the Tauri event API (listen/emit) and other core defaults. Without this, backend->frontend events (connected, message, file, status) never reach the UI.","local":true,"windows":["main"],"permissions":["core:default","notification:default"]}}